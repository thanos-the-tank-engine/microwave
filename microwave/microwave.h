//headers for the microwave stuff
//not much here, eh?

//main cli init function
int main_cli_init(void);

//expose ticker start and stop functions to enable se in seperate files
void startTicker(int);
void stopTicker(void);